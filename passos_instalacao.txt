With Ubuntu 14.04.5 x64, insert these commands:

- Install unzip: $ sudo apt-get -y install unzip master
- Unzip file: unzip master.zip
- Update packages repository: $ sudo apt-get update
- Install python: $ sudo apt-get install python-dev
- Install pip to install python packages: $ sudo apt-get -y install python-pip
- Install virtualenvwrapper to create specific packages context: $ sudo pip install virtualenvwrapper
- Add on bashrc: source /usr/local/bin/virtualenvwrapper.sh
- Sourcing bashrc: $ source ~/.bashrc
- Install ipython: $ sudo pip install IPython==5.0
- Install setuptools to configure web application python: $ sudo pip install -U pip setuptools
- Install git: $ sudo apt-get install git 
- Clone repository: $ git clone https://github.com/josepedro/url_shortener.git
- Go to project folder: $ cd url_shortener
- Creating virtual environment dependencies: $ mkvirtualenv url_shortener_env
- Install dependencies for python: $ pip install -e .
- Preparing database: $ python prepare_database.py
- Running tests: $ python setup.py test

Running on http://0.0.0.0:8000/ (Press CTRL+C to quit)

instalando ambiente para virtual => sudo pip install virtualenvwrapper
arrumando o bashrc para virtualenv_warpper => source /usr/bin/virtualenvwrapper.sh
depois da um source no bashrc
instalar o flask: sudo pip install flask
instalar o ipython: sudo pip install IPython==5.0
sem sudo: pip install flask
sem sudo: pip install IPython==5.0
instalando setuptools: pip install -U pip setuptools
rodando o teste: python setup.py test
instalando independencias do setuptools: pip install -e .
install setuptools para gerenciar dependencias e testes: pip install -U pip setuptools
instalando independencias do setuptools: pip install -e .
